class Foo [
    fields
    method doStuff(x,y,z) with locals r:
        if (x < y): {
            r = ((x + y) + z)
        } else {
            r = ((x + y) + z)
        }
        print(r)
        return ((x + y) + z)
]
main with:
    x = @Foo
    print(^x.doStuff(2,3,4))