class Stacker [
    fields
    method do(stk) with locals x, v:
        x = 20
        while (x > 0): {
            _ = ^stk.push(x)
            x = (x - 1)
        }
        v = ^stk.pop()
        while (v != 0): {
            print(v)
            v = ^stk.pop()
        }
]